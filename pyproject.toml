[project]
name = "kafka-mcp-server"
version = "0.1.0"
description = "Kafka MCP server using FastMCP"
requires-python = ">=3.10"
dependencies = [
  "fastmcp",
  "aiokafka",
  "pydantic",
  "python-dotenv",
]

[project.scripts]
start-server = "app.main:run"

[tool.uv]
package = false
